info:
  name: testPost
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8100/testpost
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
