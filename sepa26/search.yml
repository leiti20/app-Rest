info:
  name: search
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8100/sepa26/search?sum=500
  params:
    - name: sum
      value: "500"
      type: query
  body:
    type: xml
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
